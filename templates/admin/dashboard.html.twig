{% extends 'base.html.twig' %}
{% block body %}
<h1>Administration</h1>
<div class="grid">
    <div class="card"><h2>Utilisateurs</h2><p>{{ users|length }} comptes</p><a href="{{ path('admin_users') }}">Gerer</a></div>
    <div class="card"><h2>Parametres</h2><p>{{ settings|length }} cles</p><a href="{{ path('admin_settings') }}">Voir</a></div>
    <div class="card"><h2>Logs</h2><a href="{{ path('admin_logs') }}">Consulter</a></div>
</div>
{% endblock %}
